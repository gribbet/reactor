{
  "name": "reactor",
  "private": true,
  "version": "1.0.0",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "lerna run build --parallel",
    "lint": "lerna run lint --parallel",
    "lint:fix": "lerna run lint:fix --parallel"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.5.5"
  }
}
